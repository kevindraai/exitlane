# A package transaction may enable this unit, but it must not start the
# provider before ExitLane has completed the appliance gateway baseline.
[Unit]
ConditionPathExists=|/run/exitlane-provider-install/mullvad-daemon-start-allowed
ConditionPathExists=|/etc/exitlane/mullvad-installation-complete
