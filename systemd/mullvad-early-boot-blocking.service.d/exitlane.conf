# ExitLane owns appliance boot and forwarded-traffic killswitch policy.
# This marker is deliberately never created by ExitLane. Keeping the
# upstream unit enabled is harmless because its firewall initializer cannot run.
[Unit]
ConditionPathExists=/run/exitlane-provider-install/mullvad-early-boot-blocking-allowed
