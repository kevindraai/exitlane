# Router integrations

Exitlane generates a generic WireGuard client configuration. Import it into UniFi, MikroTik, ASUSWRT-Merlin, OpenWrt, pfSense or OPNsense and use that platform's policy-routing rules. Router-specific exporters are planned, but the core remains neutral.
