# Security

Exitlane is early-alpha software. Do not expose the management UI to the public internet. Keep `/etc/exitlane` root-only, restrict the WebUI to a trusted management network and rotate credentials or WireGuard keys that appear in logs or screenshots.
